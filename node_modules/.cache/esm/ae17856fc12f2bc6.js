_c12‍.x([["default",()=>_c12‍.o]]);class CustomErrorHandler extends Error {
  constructor(status, msg) {
    super();
    this.status = status;
    this.message = msg;
  }

  static alreadyExist(message) {
    return new CustomErrorHandler(409, message);
  }

  static wrongCredentials(message = "Please check your username and password") {
    return new CustomErrorHandler(401, message);
  }

  static unAuthorised(message = "unathorised request") {
    return new CustomErrorHandler(401, message);
  }

  static notFound(message = "404 not found") {
    return new CustomErrorHandler(404, message);
  }
}

_c12‍.d(CustomErrorHandler);
