let JWT_TOKEN,jwt;_670‍.x([["default",()=>_670‍.o]]);_670‍.w("./../config",[["JWT_TOKEN",["JWT_TOKEN"],function(v){JWT_TOKEN=v}]]);_670‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
  static sign(payload, expiry = "60s", secret = JWT_TOKEN) {
    return jwt.sign(payload, secret, { expiresIn: expiry });
  }

  static verify(token, secret = JWT_TOKEN) {
    return jwt.verify(token, secret);
  }
}

_670‍.d(JwtService);
