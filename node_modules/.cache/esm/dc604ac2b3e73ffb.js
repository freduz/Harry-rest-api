let DEBUG_MODE,ValidationError,CustomErrorHandler;_b3e‍.x([["default",()=>_b3e‍.o]]);_b3e‍.w("./../config",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_b3e‍.w("joi",[["ValidationError",["ValidationError"],function(v){ValidationError=v}]]);_b3e‍.w("./../services/CustomeErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);


const errorHandler = (err, req, res, next) => {
  let statusCode = 500;
  let data = {
    message: "Internal server error",
    ...(DEBUG_MODE === "true" && { orginalMessage: err.message }),
  };

  if (err instanceof ValidationError) {
    statusCode = 422;
    data = {
      message: err.message,
    };
  }

  if (err instanceof CustomErrorHandler) {
    statusCode = err.status;
    data = {
      message: err.message,
    };
  }

  res.status(statusCode).json(data);
};

_b3e‍.d(errorHandler);
